{
  "name": "invoice-reminder-automator",
  "version": "1.0.0",
  "description": "Invoice Reminder Automator API with Express, Prisma, and PostgreSQL",
  "main": "src/services/server.js",
  "scripts": {
    "start": "node src/services/server.js",
    "dev": "nodemon src/services/server.js",
    "build": "echo 'No build step required'",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}